import { TierIsCommonPipe } from './tier-is-common.pipe';

describe('TierIsCommonPipe', () => {
  it('create an instance', () => {
    const pipe = new TierIsCommonPipe();
    expect(pipe).toBeTruthy();
  });
});
