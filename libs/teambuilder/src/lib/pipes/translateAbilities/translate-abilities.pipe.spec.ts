import { TranslateAbilitiesPipe } from './translate-abilities.pipe';

describe('TranslateAbilitiesPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateAbilitiesPipe();
    expect(pipe).toBeTruthy();
  });
});
