import { FindAbilityNamePipe } from './find-ability-name.pipe';

describe('TranslateAbilitiesPipe', () => {
  it('create an instance', () => {
    const pipe = new FindAbilityNamePipe();
    expect(pipe).toBeTruthy();
  });
});
