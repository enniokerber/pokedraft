import { TranslatableTitle2Directive } from './translatable-title2.directive';

describe('TranslatableTitle2Directive', () => {
  it('should create an instance', () => {
    const directive = new TranslatableTitle2Directive();
    expect(directive).toBeTruthy();
  });
});
