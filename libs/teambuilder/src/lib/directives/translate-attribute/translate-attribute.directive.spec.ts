import { TranslateAttributeDirective } from './translate-attribute.directive';

describe('TranslateAttributeDirective', () => {
  it('should create an instance', () => {
    const directive = new TranslateAttributeDirective();
    expect(directive).toBeTruthy();
  });
});
