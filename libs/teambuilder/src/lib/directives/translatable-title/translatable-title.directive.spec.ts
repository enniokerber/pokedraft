import { TranslatableTitleDirective } from './translatable-title.directive';

describe('TranslatableTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new TranslatableTitleDirective();
    expect(directive).toBeTruthy();
  });
});
