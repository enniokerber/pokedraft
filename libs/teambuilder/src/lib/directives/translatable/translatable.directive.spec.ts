import { TranslatableDirective } from './translatable.directive';

describe('TranslatableDirective', () => {
  it('should create an instance', () => {
    const directive = new TranslatableDirective();
    expect(directive).toBeTruthy();
  });
});
